# Generate a production value with: openssl rand -hex 32
CODEX_GATEWAY_CONFIG_SECRET=replace-with-a-stable-random-secret

# Optional. Mount this JSON read-only into the Gateway container; never commit the file.
CODEX_GATEWAY_FIREBASE_CREDENTIALS_PATH=/run/secrets/codex-gateway-firebase.json
